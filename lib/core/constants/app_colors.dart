import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette (Deep Navy / Dark Blue)
  static const Color primary = Color(0xFF00112C); // Very dark blue from the image
  static const Color primaryLight = Color(0xFF1A2C4E);
  static const Color primaryDark = Color(0xFF000000);

  // Accent Palette (Vibrant Orange)
  static const Color accent = Color(0xFFFF5500); // Vibrant orange/red-orange
  static const Color accentLight = Color(0xFFFF8844);
  static const Color accentDark = Color(0xFFCC3300);

  // Neutrals
  static const Color background = Color(0xFFF5F7FA);
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textInverse = Colors.white;
  
  // Status
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFD32F2F);
  static const Color warning = Color(0xFFFFA000);
}
